<Page
    x:Class="DynamicDesktop.Pages.RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DynamicDesktop"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <NavigationView
        IsSettingsVisible="True" IsPaneOpen="False"
        SelectionChanged="NavigationItemSelected"
        Header="{Binding SelectedItem.Content, RelativeSource={RelativeSource Mode=Self}}">
        <NavigationView.MenuItems>
            <NavigationViewItem Icon="Home" Content="Home" Tag="HomePage" IsSelected="True" />
            <NavigationViewItem Icon="Account" Content="About" Tag="AboutPage" />
        </NavigationView.MenuItems>

        <Frame x:Name="contentFrame" x:FieldModifier="Protected" />
    </NavigationView>
</Page>
